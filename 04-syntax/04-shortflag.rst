=============
'Short' flags
=============

Flags often have an equivalent short form, which you can use with a single dash, `-`:

    .. code-block:: bash

        ls -l

Is equal to:

    .. code-block:: bash

        ls --long

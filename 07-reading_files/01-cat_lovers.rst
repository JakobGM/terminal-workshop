===========
Concatenate
===========

**cat** is commonly used to combine (concatenate) or display text files.

Syntax:
    .. code-block:: bash

        cat [options] [filenames] [-] [filenames]
       

- **cat solution**